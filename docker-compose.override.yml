services:
  purrclaw:
    build:
      target: dev
    env_file:
      - .env.development
      - .env.local
    volumes:
      - ./src:/app/src
      - ./workspace:/app/workspace
      - /app/node_modules
